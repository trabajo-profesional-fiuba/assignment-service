name: Continous Integration
run-name: CI trigger @${{ github.actor }}
on: 
  push:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build.yml
  
  test:
    needs: build
    uses: ./.github/workflows/tests.yml
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
