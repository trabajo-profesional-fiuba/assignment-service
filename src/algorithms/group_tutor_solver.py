from solver import Solver


class GroupTutorSolver(Solver):

    def __init__(sefl, groups, tutors, formatter, topics):
        super().__init__(groups, tutors, formatter)
        self._topics = avaliable_dates
